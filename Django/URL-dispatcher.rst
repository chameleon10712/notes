URL Dispatcher
=================

The URLconf doesn’t look at the request method. In other words, all request methods –
``POST``, ``GET``, ``HEAD``, etc. – will be routed to the same function for the same URL.







